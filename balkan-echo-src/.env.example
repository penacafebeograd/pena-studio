# The Cloudflare Worker that acknowledges Play subscriptions.
# See ../balkan-echo-worker/README.md for how to deploy it.
#
# Without this, the app refuses to sell a subscription at all — Play
# auto-refunds anything not acknowledged within three days, and only a
# server can acknowledge.
NEXT_PUBLIC_ACK_ENDPOINT=
